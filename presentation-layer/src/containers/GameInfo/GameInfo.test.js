import Welcome from './GameInfo';
import '@testing-library/jest-dom';
import { render, screen } from '@testing-library/react';

describe('default test suite', () => {
  it('should be true"', () => {
    expect(true).toBeTruthy();
  });
});
